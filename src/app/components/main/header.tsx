import styles from 'src/app/components/main/header.less';
import React from 'react';

const Header = () => {
  return (
    <header className={styles.header}>
        LOGO
    </header>
  );
};

export default Header;
